export default {
    // config options
    server: {
        port: 1337
    },
    build: { 
        manifest: true,
        // for later:
        // rollupOptions: {
        //     output: {
        //       entryFileNames: `assets/[name].js`,
        //       chunkFileNames: `assets/[name].js`,
        //       assetFileNames: `assets/[name].[ext]`
        //     }
        // }
    }
}